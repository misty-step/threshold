__pycache__/
.DS_Store
runs/*/artifacts/
harbor-build/
jobs/

# Rust build artifacts (Cargo.lock is committed — threshold ships as a binary)
/target

# Claude Code agent worktree staging (transient)
.claude/
.pi/
runs/*/trace.otel.json
